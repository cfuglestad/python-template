name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  ci:
    uses: cfuglestad/github-workflows/.github/workflows/python-ci.yml@main
    with:
      python-version: "3.12"
      coverage-threshold: 80
